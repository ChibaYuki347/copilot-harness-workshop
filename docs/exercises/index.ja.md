# 🧪 演習

**このサイトの残りを読み終えたあとに、自分のマシンで取り組める実践課題です。** 各演習は小さく自己完結しており、「なんとなく合っていそう」ではなく、うまくいったことが客観的に確認できる形で終わります。

!!! info "2026-06-02 のライブワークショップに出る方は"
    当日は [60 分ライブツアー](00-live-tour.md) を使ってください — 演習 01 / 03 / 06 を
    1 時間のガイド付きセッションに圧縮しています。下の番号付き演習は、深掘り用に
    あとから戻ってきてください。

## あなたの経験度マトリクス — どこから始めるか

全員が同じ場所から始めるわけではありません。当てはまる行を選んで、そこから入って
ください。

| あなたが使ったことがあるのは… | まずここから | 次に |
|---|---|---|
| **Ask モード** だけ (チャットサイドパネル、提案をコピペ) | [00 · ライブツアー](00-live-tour.md) → [01 · カスタムインストラクション](01-custom-instructions.md) | 02、次に 04 |
| **Agent モード** を数回 (ツール呼び出しを承認、ファイルを編集) | [01 · カスタムインストラクション](01-custom-instructions.md) → [03 · スキル](03-skills.md) | 04、05、06 |
| **Hooks や Skills** を本番で運用済み | [04 · MCP サーバー](04-mcp.md) → [05 · カスタムエージェント](05-agents.md) → [キャップストーン](capstone.md) | Foundry Tools MCP レシピ、マルチエージェントレシピ |
| **他の agentic CLI** (Claude Code、Cursor 等) | [01 · カスタムインストラクション](01-custom-instructions.md) を斜め読み → [Reference → VS Code vs CLI](../reference/vscode-vs-cli.md) | ギャップを拾う |

行 1 の人は先に [Ask モードと Agent モード](../getting-started/ask-vs-agent.md)
を読んでください — ライブツアーはこの比較を見た前提です。

## このトラックの構成

演習は段階的に積み重なるようにできています。順番どおりに進めると、ある演習で作った成果物を次の演習で再利用できます。さらに **キャップストーン** では、そのうち 4 つを 1 本の動くパイプラインにまとめます。

トラックは **2 つ** に分かれているので、ご自身の利用環境に合わせて止める位置を選べます:

### 🟪 トラック A — Copilot を持っている人なら誰でも（VS Code *または* CLI）

これらの演習で作る成果物は、VS Code Copilot 拡張と Copilot CLI の **両方** で動作します。同じファイル・同じ設定でどちらのホストからも読み込まれます。

| # | 演習 | レイヤー | 難易度 | 時間 |
|---|---|---|---|---|
| 1 | [カスタムインストラクション](01-custom-instructions.md) | 常時適用コンテキスト | 🟢 入門 | 約20分 |
| 2 | [プロンプトファイル](02-prompt-files.md) | 再利用可能なプロンプト | 🟢 入門 | 約25分 |
| 4 | [MCP サーバー](04-mcp.md) | 外部ツール | 🟡 中級 | 約30分 |

!!! tip "MCP の設定先はホストごとに異なります"
    MCP 演習では **CLI の `.mcp.json`** を正規の設定として進めますが、ステップ 2.5 で VS Code 相当の設定（`.vscode/mcp.json`）も併記しているので、VS Code ユーザーも同じ演習を進められます。

### 🟩 トラック B — CLI 主体（VS Code でも動くが注意点あり）

これらの演習は Track A と **同じファイル形式** を使いますが、ラボの手順は **CLI のスラッシュコマンド UX**（`/agent`、`/skills`、フックログ）と、CLI では GA だが **VS Code では Preview** の機能（フック）に依存しています。VS Code ユーザーは Agent Customizations editor や GitHub Copilot Chat 出力チャンネルにコマンドを読み替えて進められます。

| # | 演習 | レイヤー | 難易度 | 時間 | VS Code 側の注意 |
|---|---|---|---|---|---|
| 3 | [スキル](03-skills.md) | 再利用可能なワークフロー + スクリプト | 🟡 中級 | 約45分 | `.github/skills/` のワークスペーススキルは VS Code でも動作 — ラボのテスト手順は CLI 流 |
| 5 | [カスタムエージェント](05-agents.md) | 専門家への委譲 | 🟡 中級 | 約30分 | 同じ `.agent.md` が両ホストで動作。`/agent` ピッカーは CLI、VS Code はエージェントピッカー |
| 6 | [フック](06-hooks.md) | ライフサイクル自動化 | 🟡 中級 | 約30分 | VS Code 対応は **Preview**、組織ポリシーで無効化されている可能性があるため事前確認 |
| ✨ | [キャップストーン — リリースノートパイプライン](capstone.md) | すべてを組み合わせる | 🟡 中級 | 約45分 | トラック B の各レイヤーを統合。スラッシュコマンド手順は CLI 寄り |

→ 開始前に [VS Code と Copilot CLI](../reference/vscode-vs-cli.md) の対応マトリクスもご覧ください。

## 始める前に

!!! warning "スクラッチ用のリポジトリで作業してください"
    大事なリポジトリでこれらの演習を行わないでください。新しいものを 1 つ用意します:

    ```bash
    mkdir ~/copilot-harness-lab && cd "$_"
    git init && git commit --allow-empty -m "init"
    ```

    どの演習も、安全にファイルを作成できる git リポジトリの中にいることを前提にしています。

必要なもの:

- **Copilot CLI** のインストールと認証。詳しくは [はじめに → インストール](../getting-started/installation.md) を参照してください。
- **`git`** と最近の shell。フックの演習では `bash` を使います。macOS / Linux / WSL のいずれでも動きます。
- **`gh` CLI**（任意）。必要なのはプロンプトファイル演習とキャップストーン演習の *発展課題* だけです。

## 各ページの構成

どの演習ページも同じテンプレートで書かれているので、毎回同じ見方で素早く読めます:

1. **学習目標** — 最後に何ができるようになるか。
2. **時間と難易度** — どの程度の作業になるか。
3. **前提条件** — 必要なファイル、アカウント、ツール。
4. **チェックポイントコミット** — `git commit -am "before-exercise"` をしておくと、やり直したくなったときに `git reset --hard` できます。
5. **シナリオ** — 問題設定を 1 段落で示します。
6. **手順** — 番号付きで並び、各手順に確認できる結果があります。
7. **完了条件** — 客観的なチェックリストです（コマンド、ファイル、終了コードなど）。
8. **参考解答** — 折りたたみ式です。[`examples/`](https://github.com/ChibaYuki347/copilot-harness-workshop/tree/main/examples) のスニペットを優先しているので、実際のファイルをそのままコピーできます。
9. **クリーンアップ** — 追加したものを削除する方法です。
10. **トラブルシューティング** — 起こりやすい失敗パターンを 2〜3 個に絞って扱います。
11. **やってはいけないこと** — あとで効いてくる落とし穴です。
12. **発展課題** — さらに広げるための 1〜3 個のアイデアです。

## 検証について

ほとんどの演習は「Copilot の返答が *それっぽいか*」ではなく、**コマンド** で検証します。繰り返し登場する便利な組み込み確認方法は次のとおりです:

| コマンド | 分かること |
|---|---|
| `/env` | このセッションでどのインストラクション / スキル / エージェント / フック / MCP サーバーが読み込まれたか。 |
| `/instructions` | 現在のスコープに入っているインストラクションファイル。 |
| `/skills` | 読み込まれているスキル。 |
| `/mcp show` | 接続されている MCP サーバーと、そのツール。 |
| `/agent` | 使えるものを確認できるカスタムエージェントピッカー。 |
| `copilot --debug …` | ツール呼び出し、フック発火、MCP 通信のストリーム。 |

正しいファイルが読み込まれていれば `/env` に表示されます。表示されないなら、問題は **検出** であってモデルではありません。

## クリーンアップ

各演習の最後には **クリーンアップ** ブロックがあります。途中で止めた場合の最短リセットは次のとおりです:

```bash
git restore --staged . && git checkout -- . && git clean -fd
```

…スクラッチ用のリポジトリ内でこれを実行します。演習で作成したファイルをすべて元に戻せます。

---

準備ができたら、**[1 · カスタムインストラクション](01-custom-instructions.md)** から始めてください。
